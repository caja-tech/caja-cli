type Request struct {
	method String
	path   String
}

let req = Request { method: "GET", path: "/health" }
let count = 5
let ok = true

let summary = "${req.method} ${req.path} count=${count} ok=${ok}"
