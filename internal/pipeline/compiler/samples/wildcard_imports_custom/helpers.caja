type Point struct {
	x Number
	y Number
}

union Shape = Point

let add = fn(a: Number, b: Number) -> Number {
	return a + b
}

let double = fn(n: Number) -> Number {
	return n * 2
}

private let hidden = 99
